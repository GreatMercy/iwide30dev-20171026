export default {
  // 检测是否配置会员等级
  hasConfMemberLevel (state) {
    return Object.keys(state.confMemberLevels).length > 0
  },
  // 检测是否配置支付方式
  hasConfPayWays (state) {
    return Object.keys(state.confPayWays).length > 0
  }
}
